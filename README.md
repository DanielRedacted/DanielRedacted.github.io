# danielredacted.net

Daniel Redacted's personal site. Astro 5 static export, deployed to GitHub Pages on push to `main`.

```bash
nvm use          # Node 20
npm install
npm run dev      # develop
npm run build    # static build -> dist/
```
